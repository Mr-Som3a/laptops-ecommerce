import React from 'react';
import Button from './_button';

function AddToCart() {
    return (
        <Button className='' action={()=>{}}>
            Add To Cart
        </Button>
    );
}

export default AddToCart;